# Copy these keys into your real .env (which is git-ignored and never edited here).
# MongoDB connection string. For Atlas use the SRV string from the Atlas UI.
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net
# Database the generators write into (and the connector reads collections from).
MONGODB_DATABASE=connector_test
